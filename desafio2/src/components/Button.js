export default function Button(props) {
    
    const handleClick = () => {
        
    }
    
    return (
        <>
            <button onClick={handleClick} >{props.text}</button>
        </>
    )
}

/* export { Button } */